# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## About This Repository

This directory (`C:\Users\CCLNe\claude`) serves as a general-purpose Claude Code workspace. It does not contain a specific project — work here may span multiple languages, frameworks, or experimental scripts.

## Context

The git repository is rooted at the Windows user home directory (`C:\Users\CCLNe`). The `claude/` subdirectory is the active working directory for Claude Code sessions.
